# AeroGlide — Frontend

Next.js 16 (App Router) frontend for AeroGlide. See the [project README](../README.md) for full setup instructions, environment variables, and architecture notes.

```bash
npm install
cp .env.local.example .env.local
npm run dev   # http://localhost:3000
```
